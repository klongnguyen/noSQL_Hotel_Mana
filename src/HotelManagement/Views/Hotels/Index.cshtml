@model HotelManagement.ViewModels.HotelListViewModel
@{
    ViewData["Title"] = "Quản Lý Khách Sạn";
}

<div class="container-fluid px-0">
    <!-- Tiêu đề & Breadcrumb -->
    <div class="d-md-flex justify-content-between align-items-center mb-4">
        <div>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-1 text-muted small">
                    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index" class="text-decoration-none">Dashboard</a></li>
                    <li class="breadcrumb-item text-secondary">Quản lý cơ sở</li>
                    <li class="breadcrumb-item active text-dark fw-semibold" aria-current="page">Khách sạn</li>
                </ol>
            </nav>
            <h2 class="h3 fw-bold text-dark mb-0">Quản Lý Danh Mục Khách Sạn</h2>
        </div>
        <div class="mt-3 mt-md-0 d-flex gap-2">
            <span class="badge bg-dark bg-opacity-75 text-white font-monospace p-2 d-flex align-items-center">
                <i class="bi bi-database me-1"></i>
                @(string.IsNullOrEmpty(Model.SelectedCity) ? "TABLE: hotels" : $"TABLE: hotels_by_city (PK: '{Model.SelectedCity}')")
            </span>
        </div>
    </div>

    <!-- Thanh công cụ tìm kiếm & lọc Admin -->
    <div class="card border-0 shadow-sm rounded-3 mb-4">
        <div class="card-body p-3 p-md-4">
            <form method="get" asp-controller="Hotels" asp-action="Index" class="row g-3 align-items-end">
                <!-- Lọc Thành phố (Query-First Partition Key) -->
                <div class="col-md-4 col-lg-3">
                    <label class="form-label small fw-bold text-secondary mb-1">
                        <i class="bi bi-geo-alt text-danger me-1"></i> Khu Vực / Thành Phố (PK):
                    </label>
                    <select name="city" class="form-select form-select-sm border-secondary-subtle">
                        <option value="">-- Tất cả thành phố --</option>
                        @foreach (var c in Model.AvailableCities)
                        {
                            <option value="@c" selected="@(Model.SelectedCity == c)">@c</option>
                        }
                    </select>
                </div>

                <!-- Lọc Số sao -->
                <div class="col-md-3 col-lg-2">
                    <label class="form-label small fw-bold text-secondary mb-1">
                        <i class="bi bi-star-fill text-warning me-1"></i> Hạng Sao:
                    </label>
                    <select name="starRating" class="form-select form-select-sm border-secondary-subtle">
                        <option value="">-- Tất cả --</option>
                        <option value="5" selected="@(Model.SelectedStarRating == 5)">5 Sao (★★★★★)</option>
                        <option value="4" selected="@(Model.SelectedStarRating == 4)">4 Sao (★★★★☆)</option>
                        <option value="3" selected="@(Model.SelectedStarRating == 3)">3 Sao (★★★☆☆)</option>
                    </select>
                </div>

                <!-- Tìm kiếm từ khóa -->
                <div class="col-md-3 col-lg-4">
                    <label class="form-label small fw-bold text-secondary mb-1">
                        <i class="bi bi-search text-primary me-1"></i> Tìm Kiếm Tên / Địa Chỉ:
                    </label>
                    <input type="text" name="search" value="@Model.SearchKeyword" class="form-control form-select-sm border-secondary-subtle" placeholder="Nhập tên khách sạn hoặc địa chỉ..." />
                </div>

                <!-- Nút thao tác -->
                <div class="col-md-2 col-lg-3 d-flex gap-2">
                    <button type="submit" class="btn btn-primary btn-sm px-3 fw-semibold flex-grow-1 shadow-xs">
                        <i class="bi bi-funnel me-1"></i> Lọc Dữ Liệu
                    </button>
                    @if (!string.IsNullOrEmpty(Model.SelectedCity) || Model.SelectedStarRating.HasValue || !string.IsNullOrEmpty(Model.SearchKeyword))
                    {
                        <a asp-action="Index" class="btn btn-outline-secondary btn-sm px-3" title="Đặt lại bộ lọc">
                            <i class="bi bi-arrow-counterclockwise"></i>
                        </a>
                    }
                </div>
            </form>
        </div>
    </div>

    <!-- Bảng Dữ Liệu Quản Trị (Admin Data Table) -->
    <div class="card border-0 shadow-sm rounded-3 overflow-hidden">
        <div class="card-header bg-white py-3 px-4 border-bottom d-flex justify-content-between align-items-center">
            <div class="fw-bold text-dark">
                <i class="bi bi-table me-2 text-primary"></i>Danh Sách Cơ Sở Lưu Trú
                <span class="badge bg-primary-subtle text-primary border border-primary-subtle ms-2">
                    @Model.TotalCount bản ghi
                </span>
            </div>
            <small class="text-success font-monospace">
                <i class="bi bi-check-circle-fill me-1"></i>Zero ALLOW FILTERING
            </small>
        </div>

        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light text-secondary small text-uppercase fw-semibold">
                    <tr>
                        <th style="width: 60px;" class="text-center">#</th>
                        <th style="width: 120px;">Mã KS</th>
                        <th>Tên Khách Sạn</th>
                        <th style="width: 170px;">Thành Phố</th>
                        <th>Địa Chỉ</th>
                        <th style="width: 120px;" class="text-center">Hạng Sao</th>
                        <th style="width: 140px;">Hotline</th>
                        <th style="width: 180px;" class="text-end pe-4">Thao Tác</th>
                    </tr>
                </thead>
                <tbody class="border-top-0">
                    @if (Model.Hotels.Any())
                    {
                        int stt = 1;
                        foreach (var hotel in Model.Hotels)
                        {
                            <tr>
                                <td class="text-center text-muted small">@stt</td>
                                <td>
                                    <span class="badge bg-secondary-subtle text-secondary-emphasis font-monospace px-2 py-1">
                                        @hotel.HotelId
                                    </span>
                                </td>
                                <td>
                                    <div class="fw-bold text-dark">@hotel.HotelName</div>
                                    <small class="text-muted font-monospace" style="font-size: 0.72rem;">PK: @hotel.HotelId</small>
                                </td>
                                <td>
                                    <span class="badge bg-info-subtle text-info-emphasis border border-info-subtle px-2 py-1">
                                        <i class="bi bi-geo-alt me-1"></i>@hotel.City
                                    </span>
                                </td>
                                <td class="text-secondary small">@hotel.Address</td>
                                <td class="text-center">
                                    <span class="badge bg-warning-subtle text-warning-emphasis fw-bold px-2 py-1">
                                        @hotel.StarRating ★
                                    </span>
                                </td>
                                <td class="font-monospace small text-secondary">
                                    <a href="tel:@hotel.Phone" class="text-decoration-none text-secondary">
                                        @hotel.Phone
                                    </a>
                                </td>
                                <td class="text-end pe-4">
                                    <div class="btn-group btn-group-sm">
                                        <a asp-controller="Hotels" asp-action="Details" asp-route-id="@hotel.HotelId" 
                                           class="btn btn-outline-primary" title="Xem hồ sơ khách sạn">
                                            <i class="bi bi-eye"></i> Hồ sơ
                                        </a>
                                        <a asp-controller="Rooms" asp-action="Index" asp-route-hotelId="@hotel.HotelId" 
                                           class="btn btn-outline-success" title="Quản lý phòng nghỉ (Module M3)">
                                            <i class="bi bi-door-open"></i> Phòng
                                        </a>
                                    </div>
                                </td>
                            </tr>
                            stt++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="text-center py-5">
                                <div class="text-muted">
                                    <i class="bi bi-inbox fs-1 d-block mb-2 text-secondary"></i>
                                    <h6 class="fw-bold">Không tìm thấy khách sạn nào phù hợp</h6>
                                    <p class="small text-secondary mb-3">Vui lòng thử lại với thành phố hoặc từ khóa khác.</p>
                                    <a asp-action="Index" class="btn btn-outline-primary btn-sm">Xem tất cả</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="card-footer bg-white py-3 px-4 border-top text-muted small d-flex justify-content-between align-items-center">
            <span>Hiển thị <strong>@Model.TotalCount</strong> bản ghi từ CSDL Cassandra.</span>
            <span>Mô hình: <strong>Query-Driven Denormalization</strong></span>
        </div>
    </div>
</div>
